# C2.5 — Documentation API, Installation, RGPD et Accessibilité

## CareerAI

**Projet :** CareerAI  
**URL :** https://www.careerai.live  
**Dépôt :** https://github.com/ARTHURNJOUONANG/Projet_Chat_board  
**Version du dossier :** Juin 2026

---

## Sommaire

1. [Référence API](#1-référence-api)
2. [Installation et exploitation](#2-installation-et-exploitation)
3. [RGPD et accessibilité](#3-rgpd-et-accessibilité)

---

<div class="page-break"></div>

## 1. Référence API

Spécification machine : [05-api-openapi.yaml](05-api-openapi.yaml) (OpenAPI 3.0).

Visualisation : importer le YAML dans [Swagger Editor](https://editor.swagger.io) ou Postman.

### 1.1 Authentification

CareerAI utilise **Supabase Auth**. Après login (`/auth/login`), le navigateur envoie les **cookies de session** sur chaque requête vers `/api/*`.

| Contexte | Mécanisme |
|----------|-----------|
| Navigateur | Cookies automatiques (`credentials: 'include'` dans `frontend/lib/api.js`) |
| API externe / Postman | Non supporté nativement sans cookie de session |
| Cron | `Authorization: Bearer <CRON_SECRET>` sur `/api/cron/*` |
| Quiz candidat | Token dans l’URL `/api/candidate/quiz/{token}` — pas de login |

### 1.2 Groupes d’endpoints

#### Campagnes (`/api/campaigns*`)

| Méthode | Chemin | Description |
|---------|--------|-------------|
| GET | `/api/campaigns` | Liste campagnes utilisateur |
| POST | `/api/campaigns` | Créer campagne (`kind`: `jobs` \| `kandi`) |
| POST | `/api/campaigns/run-now` | Exécution immédiate |
| GET/POST | `/api/campaigns/profile` | Profil candidat campagne |
| GET | `/api/campaigns/report` | Rapport global |
| GET | `/api/campaigns/last-run` | Dernier run |
| GET/POST/DELETE | `/api/campaigns/{id}` | Détail / mise à jour / suppression |
| GET | `/api/campaigns/{id}/applications` | Candidatures d’une campagne |
| GET/POST/DELETE | `/api/campaigns/kandi-contacts` | Contacts Kandi |

#### Recruteur (`/api/recruiter/*`)

| Méthode | Chemin | Description |
|---------|--------|-------------|
| GET/POST | `/api/recruiter/job-postings` | Postes |
| GET/POST | `/api/recruiter/candidates` | Candidats (POST multipart CV) |
| GET/POST | `/api/recruiter/quizzes` | Quiz |
| POST | `/api/recruiter/quizzes/{id}/send` | Envoi email quiz |
| GET | `/api/recruiter/rankings` | Classements |
| POST | `/api/recruiter/relevance-score` | Score pertinence |

#### Quiz candidat (public par token)

| Méthode | Chemin | Description |
|---------|--------|-------------|
| GET | `/api/candidate/quiz/{token}` | Charger quiz |
| POST | `/api/candidate/quiz/{token}/submit` | Soumettre réponses |

### 1.3 Codes de réponse courants

| Code | Signification |
|------|----------------|
| 200 | Succès |
| 400 | Données invalides |
| 401 | Non authentifié |
| 404 | Ressource introuvable |
| 409 | Conflit |
| 500 | Erreur serveur |

<div class="page-break"></div>

## 2. Installation et exploitation

### 2.1 Prérequis

| Outil | Version |
|-------|---------|
| Node.js | 18+ (20 recommandé) |
| npm | 9+ |
| Compte Supabase | Projet avec Auth + SQL Editor |
| Clé Groq | console.groq.com |
| Resend (optionnel) | Emails quiz / campagnes / auth SMTP |

### 2.2 Installation locale

```bash
git clone https://github.com/ARTHURNJOUONANG/Projet_Chat_board.git
cd Projet_Chat_board
npm install
cp .env.example .env.local
npm run dev
```

### 2.3 Variables d’environnement (minimum)

| Variable | Description |
|----------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | URL projet Supabase |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Clé anon |
| `SUPABASE_SERVICE_ROLE_KEY` | Clé service (serveur) |
| `GROQ_API_KEY` | API IA |

### 2.4 Migrations SQL

Ordre recommandé :

1. `supabase-schema.sql`
2. `supabase-schema-saas.sql`
3. `supabase-migrations/campaigns.sql`
4. `supabase-migrations/job_campaigns_add_kind_column.sql`
5. `supabase-migrations/campaigns_kandi.sql`
6. `supabase-storage-policies.sql`

### 2.5 Production

- URL publique : **https://www.careerai.live**
- Cron sécurisé : `/api/cron/run-campaigns` avec `CRON_SECRET`
- SMTP recommandé (Resend) pour éviter `email rate limit exceeded`

### 2.6 Dépannage

| Problème | Correctif |
|----------|-----------|
| `email rate limit exceeded` | Configurer SMTP Supabase ou créer user auto-confirm |
| Colonne `kind` manquante | Exécuter `job_campaigns_add_kind_column.sql` |
| Resend domain not verified | Vérifier domaine ou utiliser `onboarding@resend.dev` en test |

<div class="page-break"></div>

## 3. RGPD et accessibilité

### 3.1 RGPD — finalités

| Finalité | Données | Base légale |
|----------|---------|-------------|
| Création de compte | Email, mot de passe (hash Supabase) | Exécution du contrat |
| Assistant IA | Messages chat, contexte CV | Exécution du contrat |
| Campagnes auto | Profil candidat, CV, logs d’envoi | Consentement + contrat |
| Recruteur | CV candidats, quiz, classements | Intérêt légitime B2B |

### 3.2 Données traitées

| Catégorie | Exemples | Emplacement |
|-----------|----------|-------------|
| Identité | Nom, prénom, email, téléphone | `candidate_profiles`, `candidates` |
| Documents | CV PDF/DOCX + texte extrait | `documents`, `uploaded_documents` |
| Suivi | candidatures envoyées, statuts | `campaign_applications`, `job_applications` |

### 3.3 Mesures techniques

- HTTPS en transit
- RLS Supabase
- Secrets côté serveur uniquement
- Contrôle d’accès par middleware
- Consentement explicite (`allow_auto_apply`)

### 3.4 Accessibilité (RGAA/WCAG)

Périmètre : signup, login, campagnes, switch langue.

| Critère | État | Note |
|---------|------|------|
| Langue de page | OK | `<html lang="fr">` |
| Labels formulaires | OK | `label htmlFor` présent |
| Contraste | Partiel | palette sombre à renforcer |
| Focus clavier | Partiel | uniforme à améliorer |
| `aria-live` toasts | À améliorer | non globalisé |

### 3.5 Plan d’amélioration priorisé

1. Ajouter `aria-live="polite"` pour notifications globales.
2. Uniformiser focus visible.
3. Vérifier contrastes WCAG AA.
4. Ajouter audit axe-core en CI.

---

## Annexes

- Spécification OpenAPI détaillée : `docs/05-api-openapi.yaml`
- Documentation complète : `docs/README.md`
- Guide mobile Capacitor : `MOBILE-CAPACITOR.md`

